## Ratatui Market Board Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward the unified next-gen live workstation goal.
- Upgraded the Ratatui watchlist into a denser market board with rank, compact UP/DN/FL 1m movement, signed 30s flow, top-book depth, and `Q`/`T`/`!` quality badges.
- Kept the slice presentation-only and read-only: no wallet, private stream, signing, order route, ingestion, recording, or screen DSL behavior changed.
- Validation passed: `cargo fmt -p hls-tui --check`; `cargo test -p hls-tui --test ratatui_cockpit --test interactive_tui`; `cargo clippy -p hls-tui --all-targets --all-features -- -D warnings`; `cargo build -p hls-cli`; `cargo test --workspace --all-features`; scoped `git diff --check`.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered `RANK`, `FLOW30`, `DEPTH`, `Q`, and `UP`.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 224 WS messages, 476 market events, 0 reconnects, and 0 data gaps.
- Caveat: full workspace clippy is currently blocked by unrelated dirty `crates/hls-hyperliquid/tests/rest_metadata.rs` changes that import `parse_candle_snapshot` before its implementation is present.

## Ratatui Adaptive Market Board Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward the unified next-gen live workstation goal.
- Made the Ratatui watchlist width-aware: full panels keep `RANK`/`FLOW30`/`DEPTH`, while 120-column medium panels switch to compact `RK`/`PX`/`FLOW` columns so movement labels like `UP+0.57%` and `DN-1.23%` remain intact.
- Kept the slice presentation-only and read-only: no wallet, private stream, signing, order route, ingestion, recording, or screen DSL behavior changed.
- Validation passed: focused TUI tests, scoped TUI clippy, full workspace tests, full workspace clippy, rebuilt CLI fixture smoke, and 120-column live top-10 smoke.
- Fixture proof: `COLUMNS=120 LINES=36 ./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered compact `RK`, `PX`, `FLOW`, and full `UP+0.57%`.
- Short public live top-10 smoke passed: `COLUMNS=120 LINES=36 ./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 235 WS messages, 485 market events, 0 reconnects, and 0 data gaps.

## Ratatui Status Drilldown Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward the unified next-gen live workstation goal.
- Focused `status` pane now renders a real operational drilldown on narrow layouts instead of falling back to DETAIL.
- Status drilldown shows stream, recorder, health counters, view/pane/density/chart state, screen mode, row count, display pause/live state, controls, and read-only safety boundary.
- Kept the slice presentation-only and read-only: no wallet, private stream, signing, order route, ingestion, recording, or screen DSL behavior changed.
- Validation passed: `cargo test -p hls-tui --test ratatui_cockpit narrow_cockpit_renders_status_focus_as_operational_drilldown -- --nocapture`; `cargo test -p hls-tui --test ratatui_cockpit --test interactive_tui`; `cargo clippy -p hls-tui --all-targets --all-features -- -D warnings`; `cargo build -p hls-cli`; scoped `git diff --check`.
- Fixture proof: `COLUMNS=120 LINES=36 ./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered the Ratatui workstation at 120 columns.
- Short public live top-10 smoke passed: `COLUMNS=120 LINES=36 ./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 211 WS messages, 461 market events, 0 reconnects, and 0 data gaps.
- Caveat: full `cargo test --workspace --all-features` is currently blocked by unrelated untracked `crates/hls-cli/tests/alerts_command.rs` tests expecting an `alerts` subcommand that is not wired in this branch.

## Ratatui Explicit Color Control Slice
- Continued the unified next-gen live workstation goal with a shell-adaptive color reliability fix.
- Added `hls live --color auto|always|never`; default `auto` preserves terminal/env detection, `always` forces ANSI color, and `never` forces deterministic monochrome output.
- Preserved existing `HLS_FORCE_COLOR`, `CLICOLOR_FORCE`, `FORCE_COLOR`, `NO_COLOR`, and `TERM=dumb` behavior under `auto`.
- Kept the slice presentation-only and read-only: no wallet, private stream, signing, order route, ingestion, recording, or screen DSL behavior changed.
- Clean-worktree validation passed: `cargo test -p hls-cli live_tui_`; `cargo clippy -p hls-cli --all-targets --all-features -- -D warnings`; `cargo build -p hls-cli`; help output showed `--color <COLOR>` with possible values `auto, always, never`.
- Fixture proof: `--color always` emitted ANSI escapes and `--color never` emitted no ANSI escapes for fixture-backed `hls live --once --tui`.
- Short public live top-10 smoke passed with explicit color control: `COLUMNS=120 LINES=36 ./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui --color never` exited 0 with 10 symbols, 40 subscriptions, 256 WS messages, 513 market events, 0 reconnects, and 0 data gaps.

## Ratatui Market Internals Rail Slice
- Continued the unified next-gen live workstation goal with a denser hedge-fund-style header signal.
- Added an `INTERNALS` rail to the Ratatui header showing screened row count, up/down breadth, tradeable count, stale count, aggregate signed 30s flow, and aggregate top-book depth.
- The rail is derived only from existing public `FeatureSnapshot` fields after the current screen request is applied; no private stream, wallet, signing, order route, ingestion, recording, or screen DSL behavior changed.
- Validation passed: `cargo test -p hls-tui --test ratatui_cockpit cockpit_header_renders_market_internals_rail -- --nocapture`; `cargo test -p hls-tui --test ratatui_cockpit --test interactive_tui`; `cargo clippy -p hls-tui --all-targets --all-features -- -D warnings`; `cargo build -p hls-cli`; scoped `git diff --check`.
- Fixture proof: `COLUMNS=120 LINES=36 ./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui --color never` rendered `INTERNALS rows 01 ... flow -$35 depth $245`.
- Short public live top-10 smoke passed: `COLUMNS=120 LINES=36 ./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui --color never` exited 0 with 10 symbols, 40 subscriptions, 292 WS messages, 542 market events, 0 reconnects, and 0 data gaps.
