# 2026-07-08

## All-Pair TUI Detail Cards
- Created branch `feat/andrzej_tui_pair_cards`.
- Replaced selected-symbol-only detail output with `PAIR DETAIL CARDS` rendered for every visible row after filters/sorts.
- Cards are backed by real `FeatureSnapshot` fields only: price, 24h notional, bid/ask, mid/mark, spread, TOB depth/imbalance, 1m/5m/1h returns and RV, volume/trade z-scores, liquidity/momentum/mean-reversion scores, flow, resilience, metadata, confidence, and why-ranked summary.
- Regenerated SVG screenshots and visually previewed live, metadata, and resilience PNGs under `/tmp/hlscreen-pair-cards-preview/`.
- Validation passed: `cargo test -p hls-tui --test main_table_golden`; `cargo test -p hls-cli --test live_mock --test screen_command`; `python3 scripts/generate-screenshots.py`; `cargo fmt --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `cargo build --release --workspace --all-features`; `git diff --check`; live public smoke `./target/debug/hls live --symbols @107 --duration-secs 10 --refresh-secs 5 --tui` completed with 57 WS messages, 83 market events, 0 reconnects, and 0 data gaps.
- PR #23 merged into `main` at `c7584a2`; post-merge main CI run `28957709994` passed.

## Compact Workstation TUI Mock Alignment
- Created branch `feat/andrzej_tui_workstation_mock`.
- Replaced the broad market-board/detail-card renderer with the requested compact `Hyperliquid Spot Microstructure Workstation` box table and selected-pair pane.
- Kept the table truthfully backed by current `FeatureSnapshot` fields: `flow30` is signed notional flow over the implemented 30s window, and `amihud` is a liquidity-cost proxy from spread/depth/liquidity score, not a native exchange metric.
- Regenerated SVG screenshots and previewed `live-screen.svg` and `resilience-screen.svg` with `rsvg-convert`.
- Direct fixture TUI smoke had zero stderr: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --preset thin_books --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once`.
- Validation passed: `cargo test -p hls-tui --test main_table_golden`; `cargo test -p hls-cli --test live_mock`; `python3 scripts/generate-screenshots.py`; `cargo fmt --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `scripts/check-release-packaging.sh`; `git diff --check`.

## All-Data Live Smoke And End-to-End Audit
- Created branch `feat/andrzej_all_data_e2e_audit`.
- Ran current public all-symbol Hyperliquid smoke with real live data: `allpairs-e2e-20260708-195413` under `/tmp/hlscreen-allpairs-e2e-20260708-195413` completed 180s with 308 symbols, 924 subscriptions, 59,384 WS messages, 67,192 normalized events, clean shutdown, 0 reconnects, and 0 data gaps.
- Replayed the capture twice: first wrote the confidence baseline, second passed replay parity with zero drift/missing/extra rows. Screened the same run with `thin_books` and `flow_pressure`, both with zero stderr.
- Fixed a TUI truthfulness issue: the confidence detail counter labeled `gap` was actually counting incomplete feature windows. It now renders `window:N stale:N sparse:N reconnect:N parser_drop:N`.
- Post-fix 60s all-symbol smoke `allpairs-postfix-20260708-195842` completed with 18,470 WS messages, 26,156 normalized events, clean shutdown, 0 reconnects, and 0 data gaps.
- Generated real-data TUI screenshot: `/tmp/hlscreen-allpairs-e2e-20260708-195413/screenshots/live-flow-pressure-real.png`; regenerated committed deterministic screenshot SVGs.
- Validation passed: `cargo test -p hls-tui --test main_table_golden --test confidence_pane`; `cargo fmt --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `cargo build --release --workspace --all-features`; `scripts/check-release-packaging.sh`; `python3 scripts/generate-screenshots.py`; `git diff --check`; negative probes for invalid DSL, unknown preset, missing fixture, and unsupported Parquet.

## Production Docs And Live Readiness Refresh
- Created branch `feat/andrzej_production_docs_live_audit`.
- Ran latest public all-symbol Hyperliquid smoke with real live data: `allpairs-prodreadiness-20260708-201752` under `/tmp/hlscreen-prodreadiness-20260708-201752` completed 300s with 308 symbols, 924 subscriptions, 99,162 WS messages, 106,980 normalized events, 5 raw files, 1 normalized file, clean shutdown, 0 reconnects, and 0 data gaps.
- Replay parity wrote a baseline and then passed with 0 confidence drift, 0 missing rows, and 0 extra rows. `thin_books` and `flow_pressure` screened the captured data with clean stderr.
- Found and fixed a TUI truthfulness issue: visible rows missing spread or top-of-book depth evidence could still show `quality good`. Quality now reports `partial` for incomplete visible quote/depth coverage, and the header says `p95 row age` instead of `p95 local`.
- Post-fix 60s all-symbol smoke `allpairs-prodreadiness-postfix-20260708-202420` completed with 18,791 WS messages, 26,455 normalized events, clean shutdown, 0 reconnects, 0 data gaps, `p95 row age`, and `quality partial` for sparse visible coverage.
- Updated README, docs index, open-source checklist, microstructure docs, a new `docs/production-readiness.md`, diagrammed `docs/architecture.md`, and `docs/reports/2026-07-08-production-readiness-live-refresh.md`.
- Generated real-data TUI screenshot: `/tmp/hlscreen-prodreadiness-20260708-201752/screenshots/flow-pressure-live.png`; regenerated committed deterministic screenshot SVGs.
- Validation passed: `cargo fmt --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `cargo build --release --workspace --all-features`; `scripts/check-release-packaging.sh`; `python3 scripts/generate-screenshots.py`; Markdown local-link check; `rsvg-convert docs/assets/screenshots/live-screen.svg -o /tmp/hlscreen-prodreadiness-preview-live.png`; `git diff --check`; negative probes and read-only/private-surface scans.

## Live Spot Symbol Display Mapping Fix
- Created branch `feat/andrzej_symbol_display_mapping`.
- Reproduced the operator report against live Hyperliquid metadata: `spotMeta.universe[107].name` is `@107`, tokens `[150, 0]` derive `HYPE/USDC`, and `spotMetaAndAssetCtxs` contexts carry a `coin` field that must be used for joins.
- Fixed parser semantics: display pairs now derive from token names while feed IDs remain official transport IDs; asset contexts are joined by `coin`, not array position.
- Fixed CLI/TUI behavior: `hls live --symbols HYPE/USDC`, `hls live --symbols hype-usdc`, and `hls live --symbols @107` resolve to feed ID `@107`; metadata-backed TUI rows show `HYPE/USDC` instead of `@107`.
- Live validation: `hls symbols --include hype-usdc --top 1` printed `HYPE/USDC @107`; `hls symbols --include ueth-usdc --top 1` printed `UETH/USDC @151`; `ETH/USDC` correctly failed as unknown in current Hyperliquid public metadata.
- Short live TUI smoke: `./target/debug/hls live --symbols hype-usdc --duration-secs 5 --refresh-secs 5 --tui` completed with 35 WS messages, 63 market events, 0 reconnects, 0 data gaps, and rendered `HYPE/USDC`.
- Validation passed: focused parser/CLI/TUI tests, `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --all-features`, debug/release builds, release packaging, screenshot generation, Markdown links, and `git diff --check`.

## Next-Gen Keyboard-Interactive TUI
- Created branch `feat/andrzej_nextgen_interactive_tui`.
- Added pure `hls-tui::interaction` state for row focus, view tabs, density, help, pause, and clean quit. Tests cover keyboard actions and renderer behavior without terminal I/O.
- Upgraded the workstation renderer with a command rail, selected-row marker, focused row detail, and view-specific panes for overview, flow, quality, metadata, and explain. The renderer still consumes real `FeatureSnapshot` fields only and keeps read-only/no-advice copy visible.
- Wired `hls live --tui` to direct Crossterm raw-mode polling only when stdin and stderr are real TTYs. Non-TTY smokes and fixture screenshots do not wait for input.
- Fixed a live-progress display-name regression found during smoke: progress refresh frames now attach public metadata before rendering, so `hype-usdc` shows `HYPE/USDC` rather than feed ID `@107`.
- Regenerated committed deterministic SVG screenshots and previewed `docs/assets/screenshots/live-screen.svg` as `/tmp/hlscreen-nextgen-tui-preview/live-screen.png`.
- Final real public smoke: `./target/debug/hls live --symbols hype-usdc --duration-secs 10 --refresh-secs 5 --tui` completed with 61 WS messages, 105 market events, 0 reconnects, 0 data gaps, and display-name-correct progress/final output.
- Validation passed: `cargo test -p hls-tui --test interactive_tui --test main_table_golden`; `cargo test -p hls-cli --test live_mock`; `python3 scripts/generate-screenshots.py`; `cargo fmt --check`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `cargo build --release --workspace --all-features`; `scripts/check-release-packaging.sh`; `git diff --check`.

## TUI Width-Adaptive Live Rendering
- Created branch `feat/andrzej_tui_width_adaptive`.
- Diagnosed the screenshot corruption as terminal line wrapping, not bad market data: live `--tui` was using the fixed 107-column deterministic workstation renderer plus unbounded detail/progress lines in a narrower terminal.
- Added width-aware `hls-tui` rendering options with compact, narrow, and mini column sets. Live TTY progress/final frames now pass Crossterm terminal width and bound selected-detail lines.
- Follow-up screenshot showed live mode still selecting the wide headers because the reported terminal width was too optimistic for the actual visible pane. Live TTY rendering is now capped to a conservative 96 columns with an 8-column safety margin and a tiny fallback layout.
- Kept existing default deterministic rendering stable for screenshot/golden artifacts unless a terminal width is explicitly supplied.
- Validation passed: `cargo test -p hls-tui --test interactive_tui --test main_table_golden`; `cargo test -p hls-cli --test live_mock`; `cargo fmt --check`; `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features`; `git diff --check`.

## Ratatui Live TUI Rewrite Checkpoint
- Created branch `feat/andrzej_ratatui_live_tui` from the live TUI work.
- Added plan artifact `docs/superpowers/plans/2026-07-08-ratatui-live-tui.md`.
- Added `ratatui` and a new `hls_tui::ratatui_app` renderer with wide/medium/narrow layouts: `WATCHLIST`, selected `MICROSTRUCTURE`/`DETAIL`, `CHART`, `BOOK`, `TAPE`, and status bar.
- Routed fixture `hls live --once --tui`, live progress frames, default terminal live progress, and final live TTY/`--tui` output through the Ratatui renderer while preserving non-`--tui` deterministic output.
- Added alternate-screen lifecycle for real interactive terminal sessions and kept raw-mode rollback on setup failure.
- Ratatui now reflects existing keyboard state: selected row, overview/flow/quality/metadata/explain tabs, density, help overlay, pause state, and read-only status.
- Full validation passed: `cargo fmt --check`; `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `scripts/check-release-packaging.sh`; `python3 scripts/generate-screenshots.py --check`; `git diff --check`.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 10 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 275 WS messages, 525 market events, 0 reconnects, and 0 data gaps.
- Remaining gaps versus the full objective after the initial cockpit checkpoint were true OHLC/candlestick chart history, command palette, and in-TUI filter/preset/sort/timeframe editors; later slices in this branch addressed those, leaving pane focus and richer chart/order-book interaction as the main TUI gaps.

## Ratatui Real Candle Chart Slice
- Continued branch `feat/andrzej_ratatui_live_tui`.
- Carried real `CandleEvent` history from `LiveMarketState` into `RatatuiFrameModel` and replaced the placeholder synthetic sparkline with selected-symbol 1m OHLC/volume chart rendering.
- Hardened core candle state: current interval candle updates replace existing bars by `(interval, open_ts_ms)`, candle history is sorted, and per-symbol candle history is bounded to 512 entries.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered `CANDLES 1m  O 34.5000 H 35.2000 L 34.4000 C 35.0000 VOL 1200`.
- Focused validation passed: `cargo test -p hls-core --test market_state`; `cargo test -p hls-tui --test ratatui_cockpit`; `cargo test -p hls-cli --test live_mock`; `cargo clippy -p hls-core -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings`; `cargo fmt --check`; `git diff --check`.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 208 WS messages, 458 market events, 0 reconnects, and 0 data gaps.
- Remaining gaps versus the full objective at this checkpoint were command palette and in-TUI filter/preset/sort/timeframe editors; the later control-deck slice addressed those. Old string renderer remains for deterministic non-TTY output.

## Ratatui Live Control Deck Slice
- Continued branch `feat/andrzej_ratatui_live_tui`.
- Made `/`, `p`, `s`, and `t` active in live TTY mode: `/` opens a validated filter editor, `p` opens a preset editor, `s` opens a sort editor, and `t` cycles the chart window across 1m/5m/15m/30m/60m.
- Command editor submissions validate a candidate `ScreenRequest` before applying it; invalid filters keep the previous live request active and show the validation error in the TUI.
- The controls mutate only `ScreenRequest` and `WorkstationUiState`; public WebSocket subscriptions, recording, private-data boundaries, and order routes remain untouched.
- Ratatui header/help now render the active chart window/command target and no longer advertise the control keys as reserved.
- Added `HLS_FORCE_COLOR=1`/`CLICOLOR_FORCE`/`FORCE_COLOR` override support for shells that inherit `NO_COLOR=1` or `TERM=dumb`.
- Validation passed: `cargo fmt --check`; `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings`; `cargo test -p hls-tui --test interactive_tui --test ratatui_cockpit`; `cargo test -p hls-cli --test live_mock`; `cargo test -p hls-cli commands::live::tests::live_tui`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `scripts/check-release-packaging.sh`; `python3 scripts/generate-screenshots.py --check`; `git diff --check`.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered `chart:15m` and `CANDLES 1m/15m` with no `reserved` control text.
- Forced-color proof: `HLS_FORCE_COLOR=1 TERM=dumb NO_COLOR=1 ./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` emitted ANSI color escapes.
- Short public live top-10 smoke passed after rebuild: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 195 WS messages, 447 market events, 0 reconnects, and 0 data gaps.
- Remaining gap versus the full objective: pane focus and richer chart/order-book interaction still need follow-up work.

## Read-Only Localhost Server Loop Slice
- Continued the roadmap-completion goal by closing the helper-only server gap for current in-memory API state.
- Added `hls-server::serve_until_shutdown`, a small read-only HTTP/1.1 loop over `tokio::net::TcpListener` that serves existing `/health`, `/symbols`, `/screen`, and `/symbol/{symbol}` helpers and rejects unsupported/private-looking routes.
- Wired `hls server --bind <addr>` to start the loop and stop cleanly on Ctrl-C; `hls server --print-health` remains the script-friendly one-shot preview.
- Red/green test: `cargo test -p hls-server --test read_only_api long_running_server_serves_read_only_http_until_shutdown` failed before implementation on missing `serve_until_shutdown` and passed after implementation.
- Validation passed: `cargo test -p hls-server --test read_only_api`; `cargo test -p hls-cli --test health_commands`; `cargo fmt --check`; `cargo clippy -p hls-server -p hls-cli --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo build --workspace --all-features`; `git diff --check`; manual loopback smoke started `./target/debug/hls server --bind 127.0.0.1:<port> --simulate-health healthy`, curled `/health`, then shut down with SIGINT and printed `hls server stopped cleanly`.
- Boundary: this is not yet a live-updating hosted market-state daemon; the current loop serves the current read-only in-memory API state.

## Ratatui Pane Focus Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward the fully interactive workstation goal.
- Added explicit `WorkstationPane` focus state for watchlist, detail, chart, book, tape, and status panes; `[` and `]` cycle focus while `Tab` remains the detail-view cycle.
- Added basic optional mouse parity for real TTY sessions: mouse wheel maps to row movement, left-click maps terminal regions to pane focus, and command-entry mode ignores mouse events so keyboard command editing keeps priority.
- Ratatui header, status bar, help overlay, and panel border/title styling now expose focused-pane state without changing ingestion, recording, subscriptions, private-data boundaries, or screen DSL semantics.
- Command palette mode keeps priority over pane keys, so `[` and `]` remain editable filter/preset/sort characters when a command is open.
- Validation passed: `cargo fmt --check`; `cargo test -p hls-tui --test interactive_tui --test ratatui_cockpit`; `cargo test -p hls-cli commands::live::tests::live_tui`; `cargo test -p hls-cli live_tui --bin hls`; `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; scoped `git diff --check`.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered `pane:watchlist`, `[FOCUS] WATCHLIST`, and `focus watchlist`.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 271 WS messages, 535 market events, 0 reconnects, and 0 data gaps.

## Ratatui Contextual Pane Actions Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward true focused-pane interaction.
- Made `Up`/`Down` context-aware in `WorkstationUiState`: watchlist focus moves selected row, detail focus cycles detail views, and chart focus cycles chart windows without moving the selected market.
- Kept command palette priority and read-only boundaries unchanged; mouse wheel events continue to map to the same `Up`/`Down` actions, so they inherit focused-pane semantics.
- Updated Ratatui help text to describe focused-pane actions instead of claiming all arrows always move market rows.
- Validation passed: `cargo fmt --check`; `cargo test -p hls-tui --test interactive_tui --test ratatui_cockpit`; `cargo test -p hls-cli commands::live::tests::live_tui`; `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` still rendered the focused Ratatui cockpit.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 218 WS messages, 472 market events, 0 reconnects, and 0 data gaps.

## Ratatui Book And Tape Depth Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward the full workstation goal.
- Upgraded the `BOOK` pane from placeholder depth text to selected-symbol bid/ask price, size, notional, spread, top-of-book depth, imbalance, OFI, pressure meter, tradeability/resilience, adverse-selection proxy, and explicit `BOOK proxy only` labeling.
- Upgraded the `TAPE` pane from symbol/price/flow rows to selected-symbol flow context plus a screened flow/OFI leaderboard sorted by absolute signed flow. It remains truthfully labeled as a public BBO/flow proxy, not private fills or true trade prints.
- Validation passed: `cargo fmt --check -p hls-tui`; `cargo test -p hls-tui --test ratatui_cockpit`; `cargo test -p hls-tui --test interactive_tui`; `cargo test -p hls-cli commands::live::tests::live_tui`; `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings`.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered `BID`, `ASK`, `notional`, `imbalance`, `Selected flow`, `Flow leaderboard`, and `OFI`.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 227 WS messages, 482 market events, 0 reconnects, and 0 data gaps.

## Ratatui Adaptive Drilldown Slice
- Continued branch `feat/andrzej_ratatui_live_tui` toward the full workstation goal.
- Medium Ratatui layout now keeps the main cockpit together by rendering compact `BOOK` and `TAPE` panels below the chart instead of hiding them.
- Narrow Ratatui layout now uses pane focus as a drilldown switch: default focus still shows watchlist/detail, while focused `CHART`, `BOOK`, or `TAPE` replaces the lower panel.
- Validation passed: `cargo fmt --check -p hls-tui`; `cargo test -p hls-tui --test ratatui_cockpit`; `cargo test -p hls-tui --test interactive_tui`; `cargo test -p hls-cli commands::live::tests::live_tui`; `cargo clippy -p hls-tui --all-targets --all-features -- -D warnings`; `cargo test --workspace --all-features`; `cargo build --workspace --all-features`; `git diff --check`.
- Fixture proof: `./target/debug/hls live --symbols @107 --fixture-file tests/fixtures/hyperliquid/ws_mock_live.ndjson --metadata-file tests/fixtures/microstructure/metadata_enrichment.json --once --tui` rendered the full cockpit with book and tape.
- Short public live top-10 smoke passed: `./target/debug/hls live --top 10 --duration-secs 8 --refresh-secs 2 --tui` exited 0 with 10 symbols, 40 subscriptions, 226 WS messages, 477 market events, 0 reconnects, and 0 data gaps.
- Caveat: local `cargo clippy -p hls-tui -p hls-cli --all-targets --all-features -- -D warnings` is blocked by unrelated dirty `hls-store/src/parquet.rs` `clippy::useless_format` errors.
